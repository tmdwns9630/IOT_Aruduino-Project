import { Base } from "../../ContainerBase/index";
declare abstract class HashContainerBase<K> extends Base {
    protected static sigma: number;
    protected static treeifyThreshold: number;
    protected static untreeifyThreshold: number;
    protected static minTreeifySize: number;
    protected static maxBucketNum: number;
    protected bucketNum: number;
    protected initBucketNum: number;
    protected hashFunc: (x: K) => number;
    protected constructor(initBucketNum?: number, hashFunc?: (x: K) => number);
}
export default HashContainerBase;
